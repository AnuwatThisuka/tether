# Slice 3: Structured logging polish

Living ExecPlan for [`IMPLEMENT_PLAN_v0.2.md`](../IMPLEMENT_PLAN_v0.2.md).
Uses `log/slog` only — no OpenTelemetry deps in this slice.

## Purpose

Make operational logs consistently queryable: same attribute keys across
WAL fan-out, disconnects, lag drops, and shape halts. Export `WithLogger`.

## Progress

- [x] ExecPlan authored
- [x] `WithLogger(*slog.Logger)`
- [x] Standard attr keys on loud paths (slot, shape, client_id, lag_bytes, err)
- [x] Unit test: custom logger receives disconnect/lag fields
- [x] Docs + CHANGELOG
- [x] Commit / push
- [x] Move to `plans/done/` on release

## Decision Log

- Decision: Defer OpenTelemetry traces to an optional later slice (needs deps).
  Rationale: AGENTS.md short dep list; slog is enough for host log pipelines.
  Date/Author: 2026-07-19

- Decision: Export `WithLogger`; default remains `slog.Default()`.
  Date/Author: 2026-07-19

## Outcomes & Retrospective

Slice 3 accepted:

- `TestWithLogger_DisconnectIncludesAttrs`
- Loud paths include `slot` / `shape` / `client_id` / `lag_bytes` / `err`
- OTel traces deferred (need explicit dep approval)
